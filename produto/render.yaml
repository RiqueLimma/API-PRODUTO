services:
  - type: web
    name: api-produto
    env: java
    buildCommand: "cd produto && ./mvnw clean package -DskipTests"
    startCommand: "cd produto && java -jar target/*.jar"
    plan: free
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      - key: PORT
        value: 8080